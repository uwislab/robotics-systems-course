services:
  web:
    build:
      context: .
      network: host          # 构建时 pip install 需要访问互联网
    expose:
      - "80"
    restart: unless-stopped
    labels:
      # Caddy docker-proxy 路由标签
      caddy: "http://robotic.uwis.cn"
      caddy.reverse_proxy: "{{upstreams 80}}"
    networks:
      - coolify              # 让 Caddy 代理可以连接到此容器

networks:
  coolify:
    external: true           # 引用已存在的 Coolify 全局网络
